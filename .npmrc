node-linker=hoisted
# app.config.ts imports constants/app-identity.js, which Expo's config loader
# reaches via require() of an ESM file. That needs Node >= 22.12; on older Node
# it throws "Unexpected token 'export'" during config evaluation, which is a
# confusing place to discover a version problem. Fail at install instead.
engine-strict=true
